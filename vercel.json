{
  "installCommand": "npm install && npm --prefix frontend install @rollup/rollup-linux-x64-gnu",
  "buildCommand": "npm --prefix frontend run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

